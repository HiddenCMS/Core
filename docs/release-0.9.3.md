# HiddenCMS Core 0.9.3

- Restore consistent spacing between administration form fields and their action buttons.
- Keep form actions aligned, evenly spaced and able to wrap cleanly on narrow screens.
- Remove the unwanted border and background around modern form action groups.

## Compatibility

PHP >=8.1; Altitude ^0.5. Database schema remains 0.8.0; no new migration.

## Validation

Composer metadata and git diff checks pass. The corrected action layout was verified on the user creation form at a narrow viewport.
